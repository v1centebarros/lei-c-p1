print "Nome do robot: ";
TEXT name = input;
ROBOT robit = (name, 1);

NUM bc = beaconCount;

while bc > 0 do
    print "Diz a velocidade a que queres ir: ";
    NUM speed = input;
    print speed;
end
